.group_index_header
  = current_user.name
  = link_to '友人一覧', friends_path
  = link_to '友人検索', search_friends_path
  = link_to "ログアウト", destroy_user_session_path, method: :delete
- if @groups.present?
  = render partial: :group, collection: @groups, as: :group
- else
  グループはまだありません。
  = link_to '友人一覧', friends_path , class: 'fa fa-cog'
  からトークする友達を探してください。
